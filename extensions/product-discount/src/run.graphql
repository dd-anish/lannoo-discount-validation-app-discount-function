query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            id
            title
            hasAnyTag(tags: ["teacher", "journalist", "influencer"])
            metafield(namespace: "custom", key: "has_sample_copy") {
              value
            }
          }
        }
      }
    }
    buyerIdentity {
      customer {
        email
        metafield(namespace: "custom", key: "can_order_samples") {
          value
        }
        hasAnyTag(tags: ["teacher", "journalist", "influencer"])
      }
    }
  }
}
